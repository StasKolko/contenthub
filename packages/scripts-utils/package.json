{
  "name": "@packages/scripts-utils",
  "private": true,
  "type": "module",
  "exports": {
    "./project-root": "./src/project-root/index.ts"
  },
  "scripts": {
    "check:type": "tsc --noEmit",

    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@packages/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "@configs/typescript": "workspace:*",
    "@configs/vitest": "workspace:*",
    "@vitest/coverage-v8": "catalog:test",
    "vitest": "catalog:test"
  }
}
